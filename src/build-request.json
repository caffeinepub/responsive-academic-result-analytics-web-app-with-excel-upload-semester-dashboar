{
  "kind": "build_request",
  "title": "Adjust subject abbreviation rules (exclude 'and' and handle 'through')",
  "projectName": "Academic Result Analytics",
  "priority": "normal",
  "requirements": [
    {
      "id": "REQ-1",
      "text": "Update subject abbreviation generation so that the word \"and\" is not used when deriving abbreviations, and the word \"through\" is handled with a custom rule instead of the default \"first letter of each word\" behavior.",
      "target": "frontend",
      "source": {
        "messageIds": [
          "msg-3"
        ],
        "quotes": [
          "for abrivation dont take word and and through change"
        ]
      },
      "acceptanceCriteria": [
        "For a subject name containing \"and\" (case-insensitive), the abbreviation does not include a letter derived from that word (e.g., \"Research and Development\" -> \"RD\").",
        "For a subject name containing \"through\" (case-insensitive), the abbreviation uses the agreed custom mapping for that word (not the default single first letter).",
        "Existing callers of formatSubjectCodeWithAbbreviation continue to render without runtime errors, and subject codes still fall back to code-only display when the subject name is missing or unavailable."
      ]
    },
    {
      "id": "REQ-2",
      "text": "Add unit tests (or equivalent lightweight test coverage used in this codebase) for deriveSubjectAbbreviation covering: normal multi-word names, names containing \"and\", names containing \"through\", and mixed-case inputs.",
      "target": "frontend",
      "source": {
        "messageIds": [
          "msg-3"
        ],
        "quotes": [
          "for abrivation dont take word and and through change"
        ]
      },
      "acceptanceCriteria": [
        "Tests demonstrate that \"and\" is excluded from abbreviation derivation.",
        "Tests demonstrate the custom handling for \"through\".",
        "Tests cover case-insensitive matching for both words."
      ]
    }
  ],
  "constraints": [
    "Do not edit files under frontend/src/components/ui or any other immutable paths listed in SYSTEM_CONTEXT."
  ],
  "nonGoals": [
    "No changes to backend Motoko canisters or persistence.",
    "No new UI features beyond the abbreviation rule change and its test coverage."
  ],
  "imageRequirements": {
    "required": [],
    "edits": []
  }
}